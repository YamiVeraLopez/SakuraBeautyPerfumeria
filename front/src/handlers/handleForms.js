import React from 'react'

export const handleForms = () => {
    
    function handleForm(e) {
        console.log(e.target);
        let name = e.target.name
        let value = e.target.vale
    }

  return (
    <div>handleForms</div>
  )
}
